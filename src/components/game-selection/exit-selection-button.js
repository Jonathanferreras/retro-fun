import React, { Component } from 'react';

export default class ExitSelectionButton extends Component {
  render() {
    return(
      <button className="exit-selection-button">X</button>
    );
  }
}
